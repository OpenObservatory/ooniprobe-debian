# read is used in a function, not expecting user input
read-in-maintainer-script
# this is appropriately guarded for in the maintainer scripts (see: #822669)
init.d-script-not-marked-as-conffile etc/init.d/ooniprobe
# this is appropriately guarded for in the maintainer scripts (see: #822669)
init.d-script-not-included-in-package etc/init.d/ooniprobe
# these are deliberately here
dir-or-file-in-opt opt/venvs/ooniprobe*
