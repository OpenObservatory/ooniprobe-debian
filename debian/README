Debian package for ooniprobe
============================

The Debian package is currently missing the graphical user interface as it
needs several JavaScript libraries that are currently not available in Debian.
